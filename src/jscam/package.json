{
  "name": "jscam",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "babel-preset-react-native-stage-0": "^1.0.1",
    "jest-react-native": "^18.0.0",
    "react-test-renderer": "16.2.0"
  },
  "scripts": {
    "postinstall": "rm -rf onvif && git clone https://github.com/lucaszanella/onvif && npm install onvif && npm3 install node-libs-browser",
    "start": "react-native start",
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "test": "node node_modules/jest/bin/jest.js",
    "clean": "rm -rf node_modules/* onvif"
  },
  "jest": {
    "preset": "react-native"
  },
  "dependencies": {
    "react": "16.2.0",
    "react-native": "0.52.0",
    "babel-plugin-rewrite-require": "1.9.0"
  }
}
